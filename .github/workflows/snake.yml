name: Generate Contribution Snake

on:
  schedule:
    - cron: "0 0 * * *"     # runs once a day
  workflow_dispatch:         # allows manual trigger
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  generate-snake:
    runs-on: ubuntu-latest
    steps:
      - name: Generate snake animation
        uses: Platane/snk@v3
        with:
          github_user_name: Yash-Dhadve
          outputs: |
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#ff1a1a&color_dots=#050000,#300202,#5c0808,#9c1111,#ff2d2d
            dist/github-contribution-grid-snake.svg?color_snake=#ff1a1a&color_dots=#f3e4e4,#ebc1c1,#db8a8a,#c74d4d,#ff2d2d

      - name: Push snake output to the "output" branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
